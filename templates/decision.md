## <YYYY-MM-DD HH:MM> — <T-NNNN | standing>: <one-line title>

**Context.** <what was asked, by whom, and where the card or worker stood — one to three sentences; a worker's question is quoted, not paraphrased>

**Options considered.** <(a) … (b) … each with the cost it carries; `none — <why>` when the choice was forced>

**Decision.** <what was decided, in words a reader can act on without the context above>

**Basis.** <the source it rests on. When the manual §2 rule 11 fires — a third party or infrastructure you do not own, or a choice with no clear winner — a URL or ctx7 id with its read date: `https://developers.example.com/docs/x (read 2026-09-06)` or `ctx7 /org/project — <page> (read 2026-09-06)`. When it does not fire, exactly `uncited — <the trigger that did not fire>`, e.g. `uncited — repo-internal fact, no third party involved` or `uncited — the operator's word in the pane, 2026-09-06`. Never a source from memory: a remembered doc is a claim, not a Basis. And keep the uncited reason free of any URL, domain, the word ctx7, or `read`/`verified`/`checked`/`as of` followed by a date — `shepherd-metrics` counts any of those as a citation, which is the miscount this shape exists to remove>

**Confidence.** <high | medium | low — and the one fact that would change it>

**Outcome.** <backfill at retro: what actually happened, and whether the decision held>
