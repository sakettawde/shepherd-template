# Fixture manual

This file stands in for the always-loaded CLAUDE.md. Its size is what the test measures, not its words, so the paragraph below only has to exist. Shepherd is a thin orchestrator: it routes thoughts to onboarded projects, dispatches worker sessions, watches them, verifies their results and remembers. It never does project work in its own context. Tokens here are approximated as bytes divided by four.
